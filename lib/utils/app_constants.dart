import 'package:flutter/material.dart';

// 应用中的常量
class AppConstants {
  static const Color primaryColor = Color(0xFF0D47A1);
  static const double iconSize = 64.0;
  static const double cornerButtonSize = 48.0;
}